services:
  mongo:
    image: mongo:7
    container_name: zevora-mongo
    ports:
      - "27017:27017"
    volumes:
      - zevora_mongo_data:/data/db
    restart unless-stopped

volumes:
  zevora_mongo_data:
